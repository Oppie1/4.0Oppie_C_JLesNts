#include <stdio.h>
#include<stdlib.h>



int main()

{
	
	//Declare 1 uninitialized float variable to hold the total revenue. Then Declare
	//3 integer (int) variables to store the initialized priceOfPumpkin. Then a initialized int variable
	//to hold the number of sales (choose a number again easy to math on), and finally
	//an int to represent the days worked (choose easy number).
	//CODE:
	float totalRevenue;
	int priceOfPumpkin = 13;
	int pumpkinSales = 17;
	int daysWorked = 5;

	//total revenue with float data type is assigned the result of the expression below. Since
	//sales, price of pumpkin and days  worked are all declared as int, dividing them directly would cause
	//integer division - meaning any decimal portion of the result would be silently dropped.
	//To avoid this, use type casting: (float) temporarily converts each int value
	//into a float before the math is performed.

	//Create a line of code that uses typecasting again.
	//Divide average daily revenue by the days worked in order to get the average revenue per day.
	//Remember you do NOT have to use data type with the already declared float variable
	//since it is already declared earlier in program.
	//CODE:

	//Call a print function to show a sentence to the screen. Total revenue and avg daily rev to the screen.
	//CODE:

}