#include<stdio.h>
#include<stdlib.h>	


int main() {

	//Declare an integer variable called "age" to hold the user's input.
	//CODE:

	//Call a print function to prompt the user for their age. 
	//CODE:
	printf("What is your age?\n");

		//Read the int the user inputs and store it in the variable "age" while using the
		//"&" operator.
		//CODE:

	//Use an if statement to validate whether the person is greater than or equal to 18
	//If they are call a print function to say they may enter the website to the screen.
	//CODE:
	
	//Create an if statement that provides a condition where the user is under 18 and may not enter website.
	//CODE:

}