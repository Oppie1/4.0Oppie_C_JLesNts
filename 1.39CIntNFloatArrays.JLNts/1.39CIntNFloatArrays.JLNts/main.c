#include<stdio.h>
#include<math.h>



int main() {

	//Declare an uninitialized integer variable to be used as a counter in for loops.
	//CODE:

	//Declare an int array named meatBalls with 5 uninitialized elements.
	//CODE:

	//Declare an integer variable called totalMeatBalls and initialize it to "0"
	//CODE:

	//Use a for loop that first initialize i to 0, the condition that i is less than 5 and then increment by 1
	//CODE:
	
		//Call a printf() to ask user how many meatballs they ate and use format spec in 1st arg, i + 1 2nd arg.

		//Call a scanf_s()-> with frmat spec, &var[] to scan in the user input for each day.

	//Create for loop just like previous one that now scroll though each elements/i value. So it will be the
	//same as the one you just made to scan in meatBalls i=0,...
	//CODE:

		//Assign "totalMeatBalls"  to meatBalls[i] plus equals (+=);
		//CODE:
	
	//DECLARE an uninitialized INTEGER variable named avg that will be assigned a value through expression outside
	//for loop that takes the total meatballs variable and divides it by the number of days (i)/number of iterations.
		//CODE:

		//Print to screen a sentence using %d format specifier twice that matches up with 
		//totalMeatBalls as well as avg respectively in each of the two arguments.
		//CODE:

	}
