#include<stdio.h>
#include<stdlib.h>



int main()

{
	
	//Declare 4 int variables a b and c initialize two being positive, one negative.Then create one to hold weight.
	//CODE:
 
	//Call (invoke) a printf() to print int out to the screen that takes a int and a expression where
	//it divides the two positive variable as its second parameter. ("A format string", an expression.)
	//CODE:

	//Negative values behave the same way - decimal is dropped but the sign of the result is preserved.
	//Call a print function in same way as above just use a negative.
	//CODE:

	//The 'float' data type supports decimal (floating-point) values.
	//Declare two float variable initialized to decimal .0 numbers
	//CODE:

	//Call a print function that shows the first float variable format specifier as the first parameter and an 
	//expression where it divides the two floats together. float/float. A format string specifier, expression.
	//CODE:

	//Print the same expression as above with %.2f.
	//CODE:

	//modulo operator (%) returns the remainder of division, not the quotient. 
	//Call a print() that uses a sentence about the 4 watermelons weighing 10 pounds. Which is 1 more pounds than
	//you thought.
	//CODE:
}