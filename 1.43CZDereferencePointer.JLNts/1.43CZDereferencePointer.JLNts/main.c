#include<stdio.h>




int main()
{
	//Declare int variable that is initialized to 19.
	//CODE:

	//Declare a pointer variable called pTuna and initialize it to the memory address of tuna.
	//CODE:

	printf("Address \t\t Name \t Value \n");

	//Call a printf() with appropriate format specifiers and matching variables to display 
	//pTuna, "tuna", tuna. tabs matching the above. %d in 3rd arg here but %p in 3rd arg below
	//CODE:

	//Call printf() with appropriate access specifier with matching variable to display
	//the difference between the exact location of the memory address in first argument
	//vs the pTuna variable itself in the third argument. Remember %p
	//CODE:

	//Use the * pointer in front of pTuna variable and overwrite the 19 by reinitializing it to 71.
	//CODE:

	printf("\nThe new value of *pTuna is now:\n");

	//Call a print statement using an logical sentence with the appropriate int (digit)
	//access specifier to display *pTuna with matching pTuna (*) pointer variable to 
	//display what pTuna is to the screen.
	//CODE:

}